// Libs
import { combineReducers } from 'redux'

// Reducers
const reducer = (state = {}, action) => {
  switch (action.type) {
    default:
      return state
  }
}

const app = combineReducers({ reducer })

// Selectors

export default app
